import React, { useState } from 'react';
import GoogleLogin from './features/Login/GoogleLogin';

const Login = () => {

    return (
      <div>
       < GoogleLogin />
      </div>
    )
  
    }
export default
  Login